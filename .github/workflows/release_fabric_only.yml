name: Publish Fabric Release

on:
  workflow_dispatch:
    inputs:
      changelog:
        description: 'Fabric release changelog'
        required: false
        default: 'Fabric updates and bug fixes.'
      release_type:
        description: 'Release channel'
        required: true
        default: 'STABLE'
        type: choice
        options:
          - STABLE
          - BETA
          - ALPHA

jobs:
  publish-fabric:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Publish Fabric Mod
        env:
          CURSEFORGE_TOKEN: ${{ secrets.CURSEFORGE_TOKEN }}
          RELEASE_CHANGELOG: ${{ inputs.changelog }}
        # Runs the publishMods task specifically on the :fabric subproject
        run: ./gradlew :fabric:publishMods